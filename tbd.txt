

1) save name in db, and main section change to Hello <name>
2) fix cron notification of fcm



Issues left:-

Login on PC → token A stored

Login on Phone → token B stored (overwrites A)

Result:

Only the last logged-in device receives notifications

The other device becomes silent

